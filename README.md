# Supply Chain Product Tracker (AXICOV demo)


A minimal demo showing product registration, updates and verification. Uses a local JSON ledger for quick demo. Axicov integration points are marked as placeholders.


## Run locally
```bash
cd server
npm install
node index.js
# open frontend/index.html in the browser
